If you have mms you should just be able to type

$ MMS/DESC=SNMPLIB
$ MMS/DESC=SNMPAPPS

We target Alpha VMS V7.1-2 (and later). If you are only building for a
later version I think you can remove the snprintf module and use the
DECC library version instead.
